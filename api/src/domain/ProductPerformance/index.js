const { getProductPerformance } = require('./ProductPerformanceService');

/**
 * Seam (Costura) del módulo ProductPerformance.
 * Define la interfaz pública de este dominio.
 */
module.exports = {
    getProductPerformance
};
